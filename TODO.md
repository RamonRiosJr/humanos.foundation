# Humanos Foundation TODO

## Phase 16: Ecosystem Synchronization
- [ ] Monitor the Global Event Bus for the next major architectural shift.

## Phase 17: Eradicate Mock Architecture (API Defrosting)
- [ ] **Blog Post CMS Generation:** Replace the hardcoded JSON 18-post `CORNERSTONE_POSTS` list. (Decision Pending: Odoo API vs Native Markdown files for max SEO).
- [ ] **Contact & Join Odoo 'Virtual Hole':** Discard Supabase API route. Rebuild the UI to strictly use an `<iframe>` pointing to `team.humanos.foundation/contactus` to pipe leads directly into the CRM.
- [ ] **Google Analytics (GA4) Migration:** Drop PostHog. Install standard Google Analytics `gtag.js` to ensure the Foundation's SEO, traffic visibility, and usability are correctly indexed by Google.
